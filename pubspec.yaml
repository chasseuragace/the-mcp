name: the_mcp_conscious
description: A Dart MCP server that exposes filesystem and git activity to a connected LLM as a fixed set of tools, and persists a log of its own operations to disk.
version: 2.0.0+consciousness

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # No external dependencies - pure Dart consciousness

dev_dependencies:
  lints: ^3.0.0
  test: ^1.26.3

executables:
  the-mcp-conscious: main
